import React from 'react';

import { Bar } from './app-bar.styles';

const AppBar = () => (
  <Bar>
    <div>CryptoDash</div>
    <div />
    <div>Dashboard</div>
    <div>Setting</div>
  </Bar>
);

export default AppBar;
